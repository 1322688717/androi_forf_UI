<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MyDialog" parent="android:Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 设置背景模糊的透明度-->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <style name="Font">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textColor">#ffff00ff</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="updataFont" parent="Font">
        <item name="android:textColor">#388E3C</item>
    </style>

    <style name="DialogFragment" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowMinWidthMajor">100%</item><!-- 这为最宽100% -->
        <item name="android:windowMinWidthMinor">100%</item><!-- 这为最高100% -->
        <!-- 设置有遮罩层 -->
<!--        <item name="android:backgroundDimEnabled">true</item>-->
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="BottomDialogFragmnet" parent="Theme.AppCompat.Light.Dialog">
        <!--去掉标题-->
        <item name="android:windowNoTitle">true</item>
        <!--点击空白处退出-->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!--窗体背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--窗体是否浮动-->
        <item name="android:windowIsFloating">true</item>
        <!--背景是否昏暗-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--昏暗数量-->
        <item name="android:backgroundDimAmount">0.6</item>
    </style>
</resources>